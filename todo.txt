- in cli, save output as json, rm metadata flag

# documentation
- add a metadata.md in docs
- highlight code line in docs
- add readme in multiple languages
- add icon for the two themes in readme
- mention that chunker is a content aware chunkers
- check https://github.com/Shunham7995/ragparser


# next version
- add support for csv, excel and eml files in subquent version
- add support for odf with odfpy
from odf.opendocument import load
from odf import text

doc = load("my document.odt")

for paragraph in doc.getElementsByType(text.P):
    print (paragraph.getAttribute('style name'))



# what we have done
- make fx keywords case insinsitive in patterns for code chunker
- preprocess docx to split on double newline instead of single one
- rename utils to common
- update code chunker tree rendering
- add span metadata in code chunker
- add conditional import in code chunker

# done, not yet changelog
